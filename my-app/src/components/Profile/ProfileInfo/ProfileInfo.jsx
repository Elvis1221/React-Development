import React from "react";
import {ProfileLogo} from "./ProfileLog/ProfileLog";
import {ProfileCont} from "./ProfileCont/ProfileCont";

export const ProfileInfo = (props) => {
    return <div>
        <ProfileCont/>
        <ProfileLogo/>
    </div>

};
